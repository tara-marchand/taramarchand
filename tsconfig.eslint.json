// Special TS project file, used by eslint only
{
  "extends": "./tsconfig.json",
  "include": [
    // Repeated from base config's "include" setting
    "src/components/**/*",
    "src/data/**/*",
    "src/pages/**/*",
    "src/types/**/*",
    // eslint-only inclusion
    ".eslintrc.js"
  ]
}
