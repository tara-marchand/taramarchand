import { Model, ModelCtor } from 'sequelize-typescript';

// import AuthToken from '../inactive_models/AuthToken';
// import Book from '../inactive_models/Book';
// import User from '../inactive_models/User';

// const models: ModelCtor<Model>[] = [AuthToken, Book, User];
const models: ModelCtor<Model>[] = [];

export default models;
